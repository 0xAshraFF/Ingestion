# Local Environment Files

Place local secret files here if you do not want to use the repository-root `.env`.

Examples:

- `env/local.env`
- `env/provider-keys.env`

Files in this folder are ignored by git except this README. Do not commit real API keys.
